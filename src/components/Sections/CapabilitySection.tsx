import { Section } from '../Section';
// import { useAccessStore } from '../../store/accessStore';

export const CapabilitySection = () => {
    return (
        <Section title="Capability Matrix">
            <div className="text-lg opacity-80 italic">
                Capable of nothing for now.
            </div>
        </Section>
    );
};
